import React from 'react';
import {View, Text, StyleSheet} from 'react-native';

const BookingInitialScreen = () => {
    return (
        <View>
            <Text>Welcome to Booking screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default BookingInitialScreen;