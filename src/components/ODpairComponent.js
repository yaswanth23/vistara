import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const ODpairComponent = () => {
    return (
        <View>
            <Text>OD mapping screen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});

export default ODpairComponent;